<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheets/main.css">
    <link rel="stylesheet" href="/stylesheets/landing.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/modernizr/2.8.3/modernizr.min.js" type="text/javascript" async></script>
</head>
<body>

    <div class="container">
        <% if(error && error.length > 0) { %>
            <div class="alert alert-danger" role="alert">
                <%= error %>
            </div>
        <% } %>
        <% if(success && success.length > 0) { %>
            <div class="alert alert-success" role="alert">
                <%= success %>
            </div>
        <% } %>
    </div>

    <div id="landing-header">
        <h1 class="jumbotron-text">Welcome to YelpCamp!</h1>
        <hr>
        <h4 class="jumbotron-text">Start planning your next trip today</h4>
        <a href="/campgrounds" class="btn btn-lg btn-primary">View All Campgrounds</a>
        <p>
            <form class="form-inline" action="/campgrounds" method="GET">
                <div class="form-group">
                    <a href="/login"><input class="btn btn-lg" type="button" value="Login"></a>
                    <a href="/register"><input class="btn btn-lg" type="button" value="Sign Up"></a>
                </div>
            </form>
        </p>
    </div>

    <ul class="slideshow">
        <li></li>
        <li></li>
        <li></li>
        <li></li>
        <li></li>
    </ul>

</body>
</html>


<%- include("partials/footer") %>